{
    "routes": [
        {
            "method": "GET",
            "path": "/test/notify",
            "handler": "Test.sendNotification",
            "config": {
                "policies": [
                    "global.isAdmin"
                ]
            }
        }
    ]
}